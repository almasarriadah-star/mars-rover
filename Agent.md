# 🧠 Agent.md - Mars Rover V2

## 📊 نظرة عامة
- **نوع المشروع:** تطبيق ويب للتحكم في روبوت (Mars Rover) وسيرفر محلي للاتصال مع Arduino.
- **اللغة:** Python (للخلفية) و Arduino C++ (للروبوت) و JavaScript/HTML (للواجهة الأمامية).
- **الإطار:** Flask + Flask-SocketIO.
- **الإصدار:** Python 3.13.7.
- **نقطة الدخول:** [app.py](file:///C:/Users/Abdalgani/Desktop/mars-rover/app.py)

## 🌲 المخطط الشجري
mars-rover/
├── app.py                     # السيرفر الرئيسي للتحكم بالروبوت
├── config.yaml                # إعدادات النظام، الكاميرا، المحركات، الحساسات والبلوتوث
├── requirements.txt           # حزم Python المطلوبة
├── UserReport.md              # سجل التعديلات والمشاكل المحلولة
├── Agent.md                   # ذاكرة المشروع وقواعد العمل (هذا الملف)
├── arduino/                   # الكود البرمجي للأردوينو
│   ├── robot_controller/      # الكود البرمجي الأصلي للأردوينو
│   └── robot_controller_v2/   # الكود البرمجي المحدث بنية RX-priority
├── modules/                   # مكتبات ووحدات التحكم الخلفية للبايثون
│   ├── __init__.py
│   ├── bluetooth.py           # إدارة الاتصال بجهاز HC-05 عبر البلوتوث
│   ├── camera.py              # تشغيل كاميرا Pi Camera v5 أو OpenCV
│   ├── motors.py              # التحكم بالمحركات (التنقل البسيط والريليهات)
│   ├── scenarios.py           # تنفيذ سيناريوهات القيادة الآلية المسجلة
│   ├── sensors.py             # إدارة قراءات الحساسات وتحديثها
│   └── serial_comm.py         # التحكم بالاتصال التسلسلي المباشر
└── templates/                 # صفحات الواجهة الأمامية
    ├── index.html             # الواجهة الرسومية للتحكم وعرض الكاميرا والحساسات
    └── settings.html          # صفحة إعدادات الروبوت والحساسات

## 🛠️ أوامر التشغيل
| الأمر | الوظيفة |
|-------|---------|
| `python app.py` | تشغيل سيرفر Flask والبدء في مراقبة البلوتوث والكاميرا |
| `pip install -r requirements.txt` | تثبيت الحزم المطلوبة للمشروع |

## 📦 التبعيات الرئيسية
| المكتبة | الإصدار | الوظيفة |
|---------|---------|---------|
| `Flask` | 3.1.3 | خادم الويب الأساسي |
| `Flask-SocketIO` | 5.6.1 | الاتصال الفوري ثنائي الاتجاه للتحكم وعرض البيانات |
| `pyserial` | 3.5 | الاتصال التسلسلي مع Arduino عبر منفذ البلوتوث |
| `picamera2` | >=0.3.22 | تشغيل كاميرا Raspberry Pi الرسمية الإصدار 5 |
| `opencv-python-headless` | 4.13.0.92 | معالجة الصور واسترجاع الفيديو الاحتياطي |

## ✅ أفضل الممارسات المكتشفة
- **بنية RX-Priority للأردوينو**: استقبال الأوامر فوري ويجب إعطاؤه أولوية قبل إرسال الحساسات لتفادي فقدان الأوامر في SoftwareSerial.
- **تجنب العمليات الحاصرة (Blocking)**: قراءة السيريال تتم في خلفية مستقلة (Daemon Thread) للحفاظ على استجابة السيرفر.
- **فصل المهام**: الأردوينو يقوم بالتحكم المباشر في العتاد وإرسال بيانات خام، بينما يقوم الراسبيري بالمعالجة وعرض البيانات وحفظ الإعدادات.

## ⚠️ المشاكل المعروفة والحلول
| المشكلة | السبب | الحل | التاريخ |
|---------|-------|------|---------|
| فقدان أوامر السيريال | ازدحام بافر SoftwareSerial بنصف مزدوج | بنية RX-Priority للأردوينو وتقليل معدل الإرسال | 2026-06-11 |
| الكاميرا لا تعمل على Raspberry Pi v5 | عدم توافقية OpenCV مع libcamera الجديد | استخدام مكتبة `picamera2` الرسمية كخيار أساسي | 2026-06-11 |

## 🚫 أنماط يجب تجنبها
- لا تستخدم `time.sleep()` داخل خيط الاتصال الرئيسي بـ SocketIO لتجنب حظر الاتصالات الأخرى.
- تجنب إرسال أوامر تحكم المحركات بتردد عالٍ جداً دون تأخير بسيط لتفادي امتلاء البافر التسلسلي.

## 🧹 صيانة الذاكرة (Memory Hygiene)
- آخر تنظيف: 2026-06-11
- إبقاء حجم الملف أقل من 300 سطر لضمان كفاءة التوكنز.
